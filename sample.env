# ddrive backend (compose.ddrive.yaml)
DDRIVE_ENCRYPT_PASSWORD=   # set a random string to enable encryption

# gigafile-fs backend (compose.gigafile.yaml)
GIGAFILE_ENCRYPTION_KEY=   # set a random string to enable AES-256 encryption

# Gokapi (compose.gokapi.yaml)
# GOKAPI_PORT=53842
# TZ=UTC

# Cloudflare Tunnel (compose.cloudflared.yaml)
# GOKAPI_USE_CLOUDFLARE=true
CLOUDFLARED_TUNNEL_TOKEN=
